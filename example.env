# Discord application ID (Client ID) - from https://discord.com/developers/applications -> Your App -> General Information
DISCORD_APP_ID=

# Discord Client Secret - from the OAuth2 tab (NOT the bot token)
DISCORD_CLIENT_SECRET=

# Discord Bot Token - from the Bot tab in the Discord Developer Portal (used by the bot client)
DISCORD_TOKEN=

# Guild ID where webhooks/events should be delivered (recommended if the bot is in multiple guilds). Find by enabling Discord Developer Mode -> right-click server -> Copy ID
DISCORD_PRIMARY_GUILD_ID=

# Session secret for the web dashboard (use a long random string)
SESSION_SECRET=

# OAuth callback URL for Discord login (must be added in the Discord Developer Portal -> OAuth2 -> Redirects)
# Example local: http://localhost:3000/auth/discord/callback
# Example prod:   https://yourdomain.example.com/auth/discord/callback
OAUTH_CALLBACK_URL=

# Optional: override HTTP port (default 3000)
#PORT=3000

# Optional: SQLite path (default ./data/bot.sqlite)
#DB_PATH=./data/bot.sqlite
